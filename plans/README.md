# Implementation Plans

Generated by the improve skill on 2026-09-19. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

**Operator note (this repo):** do not run `git` or `pnpm`/`node` commands without the operator’s explicit permission in the chat. When blocked, stop and ask.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Add crowd.ts verification baseline | P1 | S | — | DONE |
| 002 | Fix Live recommendation for single-sensor state | P1 | S | 001 | DONE |
| 003 | Deduplicate overlapping Live polls | P1 | S | — | DONE |
| 004 | Tighten readings RLS (anon read-only + device ingest) | P1 | M | — | DONE |
| 005 | Stop tracking firmware/compile_commands.json | P2 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **002 requires 001** so the Live recommendation behavior is locked by unit tests before/as the copy fix lands.
- **003** can run in parallel with 001/002 (different files); if both touch `PackedApp.tsx`, serialize 002 then 003.
- **004** is independent of web UI plans but changes the ESP32 upload URL/headers — coordinate a firmware flash after merge.
- **005** is independent; safe anytime.

## Findings considered and rejected

- TLS `WiFiClientSecure::setInsecure()` in firmware: intentional hackathon tradeoff; revisit with cert pinning later.
- Missing `(location, created_at DESC)` index: already present on Supabase (`readings_location_created_at_idx`).
- Batch Live fetches (`location=in.(...)`): N=1 `liveSensor` today; revisit when a second sensor goes live.
- Align `POLL_MS` with 5‑minute firmware window: valid polish; deferred vs Live recommendation / RLS.
- Remove unused `react-map-gl`: valid cleanup; not in the selected default set.
- Campus JSON / seed-sim / sparklines: direction from `NEXT_STEPS.md`; product spikes, not this batch.
